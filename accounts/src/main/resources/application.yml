server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testDb
    driverClassName: org.h2.Driver
    username: sa
    password: ""
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "qa"
build:
  version: "3.0"

accounts:
  message: "Welcome to My Bank Accounts related Local APIs in Dev"
  contactDetails:
    name: "Piyush Kumar-Developer"
    email: "bWJp9@example.com"
  onCallSupport:
    -(555)-555-5555
    -(999)-999-9999